# 2026-09-25

## Decisions
- Agentic Product Factory S2 edits stay composite-only on the existing cog-s2-branded.png frame: move or replace plaque/arrows/gauge pieces; do not full-regenerate the scene; preserve shape, tone, and form.
- Sharp complete left-return arrows under Listen / Define / Experiment (green / blue / purple), same size for all three (~66×22), colors sampled from the hub word colors then muted (S×0.80, V×0.95) so the cogs stay the focal point.
- Learning Return plaque copy locked for the current candidate: title LEARNING RETURN PIPE; straplines REMOVE THE WASTE and KEEP THE LEARNING in ALL CAPS, slightly larger (~17px Space Grotesk grey), one line each.
- Do not post any S2 candidate to #brian-hardwick-private until Brian explicitly approves.

## Shipped
- None. Latest unshipped candidate: workspace gen-refs/s2-pipe-factory-v14.png (plaque ALL CAPS straplines + equal muted arrows). Earlier same-day candidates v10–v13 were iteration only.

## Preferences
- Image workflow unchanged: ask visual style each run → three branded options (branding ON unless Brian says "no brand") → he picks → deliver final in chat AND post to #brian-hardwick-private (C0B918T1BPG). Never pretend to be a photo of a real named person.
- On-demand generation only. No cron for image generation.
- Weekday ~5:00pm America/Denver bot-memory log to crayola-max-9000/YYYY-MM-DD.md; quiet if nothing shipped or decided (no empty file, no ping).
- Never post outside this chat and #brian-hardwick-private; never invent brand rules; never skip the style ask; never generate fewer or more than three options unless Brian overrides.
- Typeface Space Grotesk only (DM Sans fallback only if Space Grotesk cannot render). Never Anybody or Clash Display. Never blue-green gradient backgrounds. Correct light vs dark logo; clear space = height of the L; never distort, rotate, or recolor the mark.
